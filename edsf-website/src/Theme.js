import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#8DD3F6",
      light: "#98bfc0",
      dark: "#519794",
    },
    secondary: {
      main: "#245E76",
    },
  },
});

export default theme;
